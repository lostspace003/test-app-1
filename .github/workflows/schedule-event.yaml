name: Scheduled Events

on:
  schedule:
  - cron: '*/5 * * * *' #runs every 5 minutes

jobs:
  show-info:
    runs-on: ubuntu-latest
    steps: 
    - name: info
      run: |
        echo "This is a scheduled tasks running at every 5 minutes"
      
  
  
