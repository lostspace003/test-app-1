name: Environment Secret Example 
on: [push]
jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Use API Key
        run: echo "Athe database key is ${{ secrets.db_key }}"
